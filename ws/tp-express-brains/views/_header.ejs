<nav class="navbar navbar-expand-lg navbar-dark bg-primary px-4">
    <a class="navbar-brand" href="/">Express Brain</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarText">
        <ul class="navbar-nav mr-auto d-flex">
            <li class="nav-item active">
                <a class="nav-link active" href="/" aria-current="page">Jouer</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#">Classement</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#">Equipes</a>
            </li>
            <% if (typeof user !== "undefined" && user.role === 'admin') { %>
                <li class="nav-item">
                    <a class="nav-link" href="/users">Utilisateurs</a>
                </li>
            <% } %>
        </ul>
        <% if (typeof user === 'undefined') { %>
            <span class="navbar-text d-block w-100">
                <a class="nav-link float-lg-end" href="/login">Connexion <i class="bi bi-person-circle"></i></a>
            </span>
        <% } else { %>
            <span class="navbar-text d-block w-100">
                <a class="nav-link float-lg-end" href="/logout">Déconnexion <i class="bi bi-person-circle"></i></a>
            </span>
        <% } %>
    </div>
</nav>

<% if (locals.flashSuccess.length > 0) { %>
    <div class="alert alert-success" role="alert">
        <%= locals.flashSuccess %>
    </div>
<% } %>

<% if (locals.flashError.length > 0) { %>
    <div class="alert alert-danger" role="alert">
        <%= locals.flashError %>
    </div>
<% } %>
